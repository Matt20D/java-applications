CREATE TABLE IF NOT EXISTS application (
                              application_id numeric primary key,
                              app_name varchar,
                              description varchar,
                              owner varchar
);